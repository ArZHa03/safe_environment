name: safe_environment
description: "A plugin fot check the integrity device."
version: 1.0.0
homepage: ''

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: any

dev_dependencies:
  flutter_lints: any

flutter:
  plugin:
    platforms:
      android:
        package: com.arzha.safe_environment
        pluginClass: SafeEnvironmentPlugin
      ios:
        pluginClass: SafeEnvironmentPlugin
